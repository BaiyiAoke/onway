# 在途 Onway

v0.5.1 个人旅行工具：独立收藏地点、按天安排多个行程、估算自驾路线，并在 Android 上打开高德导航。Web 与 Android 共用 React 界面，各自保存本地数据，不连接 TREK 或业务后端。

## 开发与构建

需要 Node.js 24、npm 11。

```powershell
npm ci
npm run dev
npm run check
npm run format:check
npm run android:debug
```

开发页面为 [http://127.0.0.1:5175/#/today](http://127.0.0.1:5175/#/today)。localhost 与 127.0.0.1 是不同的 IndexedDB 存储来源。

Android 最低 **Android 12 / API 31**。版本 **0.5.1 / versionCode 6**，包名 `app.onway.personal`，Debug APK 位于 `artifacts/onway-debug.apk`。

## 使用

- **今天**：显示当天安排；日期未定或不在行程期间时明确显示首日预览。个人备注独立保存。
- **地图**：查看当前行程，点选新增、编辑和重新选点，也可输入坐标、搜索地点。全部视图默认保存到未安排，按天筛选则保存到当天。
- **计划**：创建与切换行程，调整天数、地点所属天和顺序。删除一天会将地点移回未安排，至少保留一天。
- **地点**：无需行程即可收藏、编辑、分类，按名称／地址筛选和地图查看；加入行程时生成独立副本，之后修改或删除库中的地点不影响行程。行程地点也能单独收藏入库。

每个地点一个分类。预置景点、餐饮、住宿、停车、交通；可新增、重命名或删除自定义分类。删除分类会将库与行程中的引用改为未分类，不删除地点。

手动坐标采用十进制度，**经度在前、纬度在后**；可以在经度框粘贴一对坐标。来源选 GPS／OSM（WGS84）或高德（GCJ-02）。通过本地 `gcoord` 转换统一保存为 WGS84，不调用转换 API。已保存地点再次编辑显示其 WGS84 坐标。

## 地点搜索

“地点”和“地图”页直接显示搜索框；旁边的“搜索设置”可管理供应商与 Key。地点库地图常驻页面，点击即可添加，坐标输入为次要按钮。

- **OpenStreetMap / Nominatim** 无需 Key，可在设置中选用。手动按钮或回车查询，不输入即搜。请求串行，公共服务请求开始间隔至少 1.1 秒，15 秒超时，不自动重试。服务地址可以改为兼容 Nominatim 的 HTTPS 地址。
- 本次个人构建默认 **高德地图**，已经预置用户提供的 **Web 服务**类型 Key，打开即可搜索。“搜索设置”支持修改 Key、切换服务及恢复构建默认配置；设备保存的选择优先。Android SHA1 绑定 Key 不能用于此 HTTP 接口。
- 默认值来自被 Git 忽略的 `.env.local` 中的 `VITE_AMAP_SEARCH_KEY`，模板见 `.env.example`。未配置私有默认值的新构建使用 Nominatim。真实 Key 不提交 Git，但按用户要求会进入当前 Web／APK 构建资源；这是个人构建默认值，不是服务端密钥保管。设备修改只保存在本机。
- 每次最多 10 个结果。查询只在选定服务中进行，不自动切换；查询词发送至所选服务，自己的地点备注不会随搜索提交。
- 两个提供方各缓存最近 50 个查询、有效期 24 小时，重复查询可减少请求。高德配额由账户和服务权限决定，Onway 无法显示实时余量；在控制台查看。配额、Key 或连接异常明确提示，不自动重试。
- 选中结果后仍需确认名称、分类、地址与所属天并保存；取消不产生地点。高德结果本地转换为 WGS84，保留供应商及 POI ID 供来源显示和重复提示。修改位置后清除旧位置来源。

官方说明：[Nominatim 使用政策](https://operations.osmfoundation.org/policies/nominatim/)、[高德搜索 API](https://lbs.amap.com/api/webservice/guide/api/search/)。公共 Nominatim 的整体应用流量仍须遵守政策；公开分发或多人使用前应安排自有服务或合适供应商。

## 路线与导航

一天至少两个已安排地点后，手动点击“计算自驾路线”。路线来自 [FOSSGIS 公共 OSRM](https://routing.openstreetmap.de/about.html)，按已保存地点顺序计算总里程、时间与分段，不跨天连接。坐标发送至该服务，名称和备注留在本机。

请求串行、开始间隔至少 1 秒、20 秒超时，不自动重试。保存失败可重试写入；调整地点位置、顺序或数量后显示“路线待更新”。名称、备注、地址与分类变动不影响路线。卡片默认保留指标、按钮与异常状态，计算时间和估算说明收进“详情”；数据署名与地图纠错链接在页面统一显示。

驾驶估算不含停留时间和实时路况。Android 的“高德导航”打开手机高德并传递已保存目的地，Onway 不读取设备定位；此导航 URI 不需要搜索 Key。高德实际导航可能与 OSRM 估算不同。

## 文件备份与恢复

顶部“备份”入口提供导出和恢复，四个主导航不变。

- JSON 备份包含全部行程、每天地点的顺序、地点库、自定义分类及个人备注，保留 ID 和独立副本关系。文件格式版本为 1，记录导出时间及应用版本，最大 20 MB。
- Web 发起文件下载；Android 使用系统文件选择器保存到用户选择的位置，也可从该选择器读取文件。两端格式相同，支持手动传输后互相恢复。
- 导入先校验版本、数据结构、坐标、分类引用和重复 ID，再展示当前与备份的数据量。勾选并确认后整体替换，不执行合并。取消、无效文件或保存失败不会改变原数据。
- 可在确认页面先导出当前数据。恢复期间发现本地内容已更新时，必须重新读取预览，避免覆盖新修改。
- 搜索配置与 Key 留在当前设备，不进入文件；搜索缓存不备份，恢复保留当前搜索缓存。路线缓存不迁移，恢复时清空，之后可按天重新计算。
- 文件是可读 JSON。恢复成功后刷新应用内数据，不要求重新加载网页资源；Web 离线冷启动限制保持不变。
- “编辑收藏地点”和“编辑行程地点”现在使用不同标题，后者同时显示所属行程。

## 本地数据与离线

- Web：当前浏览器来源的 `onway-local` IndexedDB；Android：应用私有目录 `onwaySQLite.db`。两端数据独立，可通过备份文件互相转移，无账号或云同步。
- 数据库表结构仍为 v1，旧个人备注键 `demo.travel-note` 不变。旅行文档 `travel.workspace` 升级为 **schemaVersion 2**，增加地点库和分类；读取 v1 时只在内存迁移，下一次成功保存才写入 v2。旧行程、ID、顺序和备注保留，不自动收藏或创建示例。
- `routes.cache` 仍为文档 v1。搜索配置和缓存是独立键，不混入行程或路线缓存。
- 保存成功后才更新共享页面；失败保留输入，读取损坏文档不清空数据。Web 写入继续使用过期快照检查与可用时的 Web Locks。
- APK 内置页面，断网可编辑已有行程、地点库、分类、坐标与备注，读取已保存路线；在线搜索与重新算路需要网络。
- 底图使用 MapLibre GL JS + OpenFreeMap Positron，保留地图署名；不支持离线地图下载。底图失败不阻塞列表和手动坐标操作。
- Web 无 Service Worker，不保证离线冷启动或首次加载尚未下载的按需模块。
- 覆盖安装保持签名及包名一致；卸载或清除应用／网站数据会删除本地资料。

不包含设备定位、自动路线优化、实时路况、收费估算、天气、AI、费用、协作或应用商店发布。

## 文档

- [架构说明](docs/architecture.md)
- [Android 开发](docs/android.md)
- [v0.5.1 验证记录](docs/verification-v0.5.1.md)
- [v0.5 验证记录](docs/verification-v0.5.md)
- [v0.4 历史验证](docs/verification-v0.4.md)
- [v0.3 历史验证](docs/verification-v0.3.md)
- [v0.2 历史验证](docs/verification-v0.2.md)
- [工程文件清单](docs/file-inventory.md)

源码：[BaiyiAoke/onway](https://github.com/BaiyiAoke/onway)，主分支 `main`。构建成功不替代真机验收。
