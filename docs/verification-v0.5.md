# v0.5 验证记录

日期：2026-09-16。范围：本地文件备份与整体恢复、事务与旧状态保护、地点编辑标题区分。

## 工程与浏览器

- 类型、Lint、生产构建、格式与差异检查通过；Vitest 18 个文件、99 项测试全部通过。
- 7 组独立 Edge 浏览器操作通过：实际下载导出、坏文件／未知版本拒绝、取消与 390px 布局、预览后其他页面保存冲突、断网恢复与旧个人备注草稿保护、收藏／行程编辑标题与跨月日期、独立浏览器互导及刷新保留。
- IndexedDB 真实事务故障测试：第二项写入故障后，已写第一项回滚；原数据完全保留，重试成功。
- Android 适配测试模拟原生连接：写入失败回滚、并发读取不观察半恢复数据、预览冲突不覆盖。原生 SQLite 实际恢复须以以下真机结果为准。
- 秘密配置不进入导出文件；搜索配置与 Key 在恢复时保留，路线缓存清空。测试在隔离上下文进行，不覆盖用户正常 Web 数据。
- MapLibre 引擎分块仍有超过 500 kB 提示，按需加载策略未改。

## APK 与真机

- Debug APK 构建成功，覆盖安装 Huawei BRA_AL00 / Android 12 / API 31，实际安装版本 0.5.0 / code 5。
- 升级前后全部 7 项 SQLite 键值一致：1 个行程、2 个收藏地点、2 个行程地点以及备注、搜索与路线缓存保留。
- APK：artifacts/onway-debug.apk，21082314 字节；SHA256：c6871911430f8e0154a76c4db3f6a8e7d30e656d6b37301c31ac7dc2d4f5c06b。
- 用户已完成系统文件导出、选取与确认覆盖；原生 SQLite 实测行程、地点库与个人备注和恢复前语义一致，搜索设置保留，路线缓存清空，恢复代次写入成功。
- 手机导出文件通过隔离 Web 的真实文件选择器导入并重新导出，业务数据完全一致；文件不含默认 Key。该 Web 上下文禁止外部网络请求。回传文件已写入手机下载目录供离线导入验收。
- 用户确认真机打开预览后取消、再次选取 Web 回传文件并覆盖恢复正常。系统核验飞行模式开启、Wi-Fi 关闭、无活动默认网络。
- Web → Android 离线恢复实测：旅行工作区、个人备注与回传文件完全一致；搜索配置不变，路线缓存清空，恢复代次与上一次不同。
- 离线强制关闭重开后全部 8 项数据库键值完全一致；通过真实 WebView 核对今天页显示原行程、7 天、2 个已安排地点，BackupFiles 原生插件已注册。
- v0.5 计划范围内的工程、跨端文件与 Android 12 真机验收完成。

## 版本与边界

- v0.4 本地提交：811d83d，feat: 实现 v0.4 地点库与搜索管理。未推送远程。
- v0.5 实现与验收记录一并归档。备份文件格式 v1，旅行文档仍为 v2，数据库仍为 v1。
- 仅整体恢复，不合并。路线需要重新计算。Web 不承诺离线冷启动；文件操作取消不改变数据。
- .tools 下截图、下载文件与 SQLite 快照仅作本次验收证据，均被 Git 忽略。
