# v0.5.1 验证记录

日期：2026-09-16。范围：视觉 Token 收口、页面样式调整、地点更多菜单，以及 USB 打包安装说明。

## 工程与版本

- `npm run check` 通过：类型检查、Lint、Vitest 18 个文件 / 99 项测试、生产构建。
- `npm run format:check` 与 `git diff --check` 通过。
- 应用版本统一为 0.5.1，Android versionCode 为 6；包配置、锁文件、User-Agent、页脚和备份元数据同步。
- 无新增第三方依赖；备份文件格式仍为 v1，旅行文档仍为 v2，数据库仍为 v1。
- MapLibre 分块超过 500 kB 的构建提示仍存在，地图按需加载策略不变。

## 本轮浏览器审阅

- 隔离 Edge 上检查今天、地图、计划、地点、备份页面：1280px 与 390px 宽度均未出现页面横向溢出。
- 字阶、卡片阴影、手机插画、下拉箭头、输入焦点和编辑面板已做视觉检查。
- 更多菜单在 1280px、390px 和 360px 宽度下位置正常；Escape 可关闭菜单，收藏重复提示可关闭。
- 开启减少动态效果偏好后，编辑面板动画时长降为 0.01ms。
- 浏览器测试未出现未捕获的页面异常；测试数据保存在隔离浏览器上下文，未修改用户 Web 数据。

## 已知问题与真机边界

- 320px 宽度下，更多按钮换行后菜单左侧越出视口约 33px，留待后续窄屏布局修正。
- 本次 Git 提交前未重新构建或安装 0.5.1 APK，也未完成本版 Android 真机视觉验收。
- 用户已确认在 Android Studio 配置 SDK 后命令可用；USB 连接、路径检查、打包、覆盖安装与启动步骤见 [Android 开发指南](android.md)。
- APK、私有搜索 Key、浏览器截图和临时检查文件不进入 Git。历史版本的 APK 与真机验证结果保留在各自验证文档中。
