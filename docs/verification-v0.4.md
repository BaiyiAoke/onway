# v0.4 验证记录

日期：2026-09-16。范围：独立地点库、分类、坐标输入、手动搜索、复制到行程、文案精简。

## 已完成

- 类型检查、ESLint、生产构建通过。
- Vitest：16 个文件、86 个测试通过，覆盖旧版本迁移只读与失败保护、多个行程副本隔离、删除分类引用、坐标转换、搜索串行限速／缓存／取消／超时、高德配额与 Key 错误。
- 独立 Edge 浏览器的 10 组操作通过：v1 读取不落盘；手动保存写入 v2；自定义分类增改删；加入两个行程；重复复制阻止与确认；390px 离线编辑和刷新；GCJ-02 转换；未保存取消；搜索结果取消回退与缓存；地点库地图及取消重新选点；删除库地点保留行程、副本收藏。
- 390px 截图检查：四栏导航、地点卡片、分类筛选、编辑表单和搜索结果无横向溢出。
- 使用用户授权的 Web 服务 Key，对公开“兰州中川国际机场”进行高德真实 HTTP 和浏览器调用，返回 OK / 10000；浏览器返回 10 条，坐标转换、保存与刷新通过；重复搜索命中缓存，无第二次请求。
- Android 平台 Key 的 HTTP 探测返回 10009 / USERKEY_PLAT_NOMATCH，未写入代码。此结果不代表 Android SDK 鉴权已验证。
- Key 未写入源码或测试脚本；隔离浏览器测试通过进程环境变量传入，真实 Key 不进入验证记录。

浏览器测试使用独立临时上下文与公开样例，不改用户真实 Web 数据。搜索配额余额未查询，成功调用只说明该 Key 当前可以调用测试接口。

- 最终布局追加 6 组浏览器回归通过：外置搜索、默认高德／次要坐标按钮、常驻地图直接选点、Key 管理与恢复默认、离线编辑、行程日期分配、重复搜索缓存。高德网络在这组回归使用公开样例响应模拟，真实调用结果见上文。
- 按用户新要求，个人构建使用 .env.local 预置默认 Key，Git 忽略该文件。新安装无需手动粘贴；设备覆盖值优先，设置可修改与恢复默认。默认 Key 会进入个人构建资源，源码、文档和版本库不包含真实值。

## APK 与真机

- Debug APK 构建成功并覆盖安装到 Huawei BRA_AL00 / Android 12 / API 31。安装包版本 0.4.0，versionCode 4。
- 升级前后数据库全部键值一致：原 2 个行程、5 个地点、个人备注和 routes.cache 均保留。读取升级未落盘覆盖原 v1 文档。
- 应用可启动并进入地点页；按用户新布局要求再次覆盖安装成功。通过应用设置恢复默认高德来源并保存，手机确认显示“高德地图”，无需手动填写 Key。
- 用户确认新布局下的搜索、收藏、分类、添加到行程、名称输入键盘及系统返回的未保存提示正常。真机数据中已存在高德来源的机场地点和独立行程副本。
- 系统核验：飞行模式开启、Wi-Fi 关闭、无活动默认网络。离线强制关闭并重开后，全部 7 项数据库键值完全一致，地点库页面正常显示。
- 地点库离线保存补验通过：用户实际保存的备注为“地点库离线备注”；关联的行程副本仍为“地点库离线验证”，两个地点 ID 不同。两端修改互不影响。保持断网强制重启后，全部 7 项键值完全一致，手机页面实际显示已保存的地点库备注。
- 恢复网络后，用户确认新关键词可以正常搜索；系统确认飞行模式关闭且已有活动默认网络。
- 真机底图恢复未单独完成最后复核：用户随后切换到其他应用，WebView 后台读取超时。本轮底图显示与重试已通过浏览器回归，真机本次恢复显示仍标记待确认。
- APK 大小 21174784 字节，SHA256：`a68e1b3df20de3d3a3cfce98867fc5ecc187258165c06902e93e3679ec1ecf2d`。
- 路径：`artifacts/onway-debug.apk`。无 server.url，页面资源随包交付。

## 限制

底图在线；公共服务可能不可用。Web 不承诺离线冷启动。地图低缩放层级时相近标记可能重叠，可放大或从列表编辑；本轮未引入聚合。搜索缓存按供应商分别保存，均为 50 条／24 小时。没有后端代理，配置 Key 只供当前个人设备使用。
