# v0.6.0 封板记录

日期：2026-09-17。基线提交为 d1cae7e，以下检查记录形成于本次版本提交前。功能范围按现有实现冻结，后续计划页定位与编排工作台调整另行规划。

封板阶段完成本地自动检查与最新 APK 构建；最新包的真机验收仍待完成。

## 冻结范围

- 分段自驾、步行、公共交通，以及独立的火车／飞机手动记录；保留交通关联、候选与正式方案分离、缓存、恢复冲突保护和旧整日路线兼容。
- 总览紧凑预览、地图上下布局、计划按天折叠与日期选择、地点库列表／网格切换；保留当前阅读与调整操作方式。
- 机场／车站分类、指定地点后插入、连续添加、地点库重复保护、备注离页保护和地图返回原日期与地点。
- 用户已有的隐藏“另建一个示例行程”修改保留；空工作区仍可主动创建示例。此次收口未修改业务实现，只同步对应测试预期与文档。

## 版本与兼容

| 项目                                    | 当前值                                              |
| --------------------------------------- | --------------------------------------------------- |
| Web／npm／Capacitor／页脚／备份应用版本 | 0.6.0                                               |
| Android                                 | versionName 0.6.0，versionCode 7                    |
| 应用包名                                | app.onway.personal                                  |
| 最低／目标 Android API                  | 31／36                                              |
| 旅行文档                                | v4，兼容 v1／v2／v3，仅内存迁移，下次成功保存才落盘 |
| 路线缓存                                | v2，保留旧整日 v1 缓存读取                          |
| 备份外层／数据库表结构                  | v1／v1                                              |

车次、航班、已选公交与待关联交通进入备份；Key、候选缓存和 UI 偏好不导出。README 中遗漏的旅行文档 v3 描述已更正为 v4。

## 本次自动检查

- npm run check：通过类型检查、ESLint、30 个测试文件的 204 项测试及生产构建。
- npm run format:check：通过。
- git diff --check、git diff --cached --check：通过。
- npm run android:debug：构建成功，包含本轮全部体验调整。
- 使用 aapt 读取 APK 实际包名、版本与 SDK；逐项比较 APK 内的 19 个 Web 文件和 dist，全部匹配。

首次检查发现旧计划页测试仍点击被隐藏的按钮；已改为验证隐藏入口、已有数据不变，并通过空状态的可见入口验证示例创建与备注保留。没有恢复隐藏按钮或模拟点击隐藏控件。

保留现有 MapLibre 大于 500 kB 分块提示及 Gradle flatDir 提示，本轮不扩展到构建架构调整。

## 浏览器验证来源

已复核 2026-09-17 上一轮的结果文件，本次没有重跑整套截图验收：

- 四页在 360／390／768／1024／1398px 的边界检查与体验流程共 33 项断言通过，无页面异常或路线请求。
- 独立导航流程 6 项通过，涵盖备注离页、浏览器返回、滚动恢复和地图返回地点焦点。
- 使用隔离浏览器与虚构数据、模拟底图；没有操作用户真实行程，不代表真实在线接口或 Android 验收。

具体范围与截图见 [体验验证记录](experience-v0.6.md) 和 [四页布局验证](ui-review-v0.6.md)。早期在线与设备验收见 [分段交通历史记录](verification-v0.6.md)，不能据此认定最新 APK 已完成设备验收。

## 最新 APK

- 文件：artifacts/onway-debug.apk，2026-09-17 构建。
- 大小：21,240,315 字节。
- SHA-256：51B21BC13F3A4470340CEE4A8B5FD15A4718A5C4FE493E56A7EBA5429281195B。
- 产物核对记录：artifacts/v0.6/seal-artifact.json。
- 使用当前个人 Debug 签名；APK 和验收产物继续按现有规则忽略，不进入 Git。

## 待完成的设备验收

本次 adb devices 未列出设备，没有执行覆盖安装。最新 APK 仍需在 Android 12+ 核对以下内容；这是设备验收剩余项，不添加功能。

1. 备份现有数据后覆盖安装，核对原行程、地点库、备注与地图服务设置保留。
2. 核对按天折叠、日期选择、交通详情、软键盘与系统返回，尤其是未保存输入保护。
3. 实际查询自驾、步行和公交；断网后读取已有结果，并保存车次／航班。
4. 通过系统文件选择器导出和恢复新备份，核对 v4 分类、航班、选定方案及待关联记录。

连接与覆盖安装命令见 [Android 指南](android.md)。本地构建通过与功能冻结不替代上述验收。

## 后续版本候选

- 重新确定计划页的编排职责：紧凑日程编辑、地点添加与插入、调序和桌面地图联动；手机保持可独立完成编排的操作流。具体方案与版本另行确定。
- 天气、全天时间编排、票务查询、自动优化顺序继续留在本版之外。

## 本次收口改动文件

- README.md：旅行文档版本与封板入口。
- docs/release-v0.6.md：本文。
- docs/verification-v0.6.md、docs/experience-v0.6.md：明确历史验证范围，链接当前封板状态。
- docs/file-inventory.md：补充收口清单。
- src/features/plan/PlanPage.test.tsx：同步隐藏示例入口的回归测试，保留可见示例入口与备注保护验证。
